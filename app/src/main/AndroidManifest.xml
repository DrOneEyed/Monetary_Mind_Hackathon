<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.loaner">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoanER"
        tools:targetApi="31">
        <activity android:name=".activities.WorkingCapitalActivity" />
        <activity android:name=".activities.BillDiscountingActivity" />
        <activity android:name=".activities.TermLoanActivity" />
        <activity android:name=".activities.CarLoanActivity" />
        <activity android:name=".activities.EducationalLoanActivity" />
        <activity android:name=".activities.PersonalActivity" />
        <activity android:name=".activities.HomeLoanActivity" />
        <activity android:name=".activities.OTPVerificationBusinessActivity" />
        <activity android:name=".activities.OTPVerficationActivity" />
        <activity android:name=".activities.EMICalculatorActivity" />
        <activity android:name=".activities.BusinessLoanActivity" />
        <activity android:name=".activities.PersonalLoanActivity" />
        <activity
            android:name=".activities.SignUpActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activities.SignInActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />
    </application>

</manifest>